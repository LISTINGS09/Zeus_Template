// Either use (_faction in ["blu_f"]) or (side == "west") etc...
// Note - Always lower case!
/*
if (_side == "west") then {
	#include "LOADOUTFILEHERE.sqf"
};
if (_side == "east") then {
	#include "LOADOUTFILEHERE.sqf"
};
if(_side == "guer") then {
	#include "LOADOUTFILEHERE.sqf"
};
*/